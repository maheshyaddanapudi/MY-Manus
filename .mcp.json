{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_PERSONAL_ACCESS_TOKEN}"
      },
      "_comment": "GitHub MCP server for repo, PR, and issue operations. Install: npm install -g @modelcontextprotocol/server-github. Set GITHUB_PERSONAL_ACCESS_TOKEN env var."
    }
  }
}
